Karma и Jasmine


есть функция it первый параметр имя теста второй async функция самого теста
С помощью TestBed создаем копонент и его инстанс
И из Jasmine берем функцию expect() и вызываем проверку типа toEqual() toContain()

Перед вызовом каждого it вызывается beforeEach() и пересоздает компонент.

